#pragma once

// Optional — WiFi is configured on the device touchscreen at first boot.
// This file is not required. Credentials are saved to flash automatically.

#define WIFI_SSID "YourWiFiName"
#define WIFI_PASSWORD "YourWiFiPassword"
